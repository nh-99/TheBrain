language: cpp
compiler: gcc
script: cd vision && make targettest && ./bin/TargetTest
sudo: true
before_install:
    - sudo apt-get update

before_script:
    - wget https://raw.githubusercontent.com/jayrambhia/Install-OpenCV/master/Ubuntu/opencv_install.sh
    - chmod +x opencv_install.sh
    - sudo ./opencv_install.sh 2.4.9
